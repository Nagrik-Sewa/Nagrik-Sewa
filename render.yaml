services:
  - type: web
    name: nagrik-sewa-backend
    env: node
    rootDir: .
    buildCommand: npm install && npm run build:server
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
