services:
  - type: web
    name: nagrik-sewa-backend
    env: node
    rootDir: .
    buildCommand: npm run build:backend
    startCommand: npm run start:backend
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
